# Border: Fixed Point Theorems - Simplex, Sperner, KKM, Brouwer 及其等价性

## Source
**Border, K.C.** (1985). *Fixed Point Theorems with Applications to Economics and Game Theory*. Cambridge University Press.

---

## I. 单纯形 (Simplexes) - Chapter 3

### 3.1 仿射独立 (Affinely Independent)

**定义**: 集合 $\{x_0, \ldots, x_n\} \subset \mathbb{R}^m$ 称为**仿射独立**的，如果：
$$\sum_{i=0}^n \lambda_i x_i = 0 \quad \text{且} \quad \sum_{i=0}^n \lambda_i = 0 \quad \Rightarrow \quad \lambda_0 = \cdots = \lambda_n = 0$$

**推论**: 如果 $\{x_0, \ldots, x_n\} \subset \mathbb{R}^m$ 仿射独立，则 $m \geq n$。

### 3.3 单纯形定义

**定义**:
- **$n$-单纯形** 是 $n+1$ 个仿射独立点的所有**严格正**凸组合的集合
- **闭 $n$-单纯形** 是 $n+1$ 个仿射独立向量的凸包

$$x_0 \cdots x_n = \left\{\sum_{i=0}^n \lambda_i x_i : \lambda_i > 0, \, i = 0, \ldots, n; \, \sum_{i=0}^n \lambda_i = 1\right\}$$

每个 $x_i$ 是 $x_0 \cdots x_n$ 的一个**顶点**，每个 $k$-单纯形 $x_{i_0} \cdots x_{i_k}$ 是它的一个**面**。

### 3.4 重心坐标 (Barycentric Coordinates)

**练习**: 如果 $y$ 属于仿射独立集 $\{x_0, \ldots, x_n\}$ 的凸包，存在唯一的数 $\lambda_0, \ldots, \lambda_n$ 使得：
$$y = \sum_{i=0}^n \lambda_i x_i$$
这些数 $\lambda_0, \ldots, \lambda_n$ 称为 $y$ 的**重心坐标**。

### 3.5 标准单纯形

**定义**: **标准 $n$-单纯形** 是：
$$\Delta^n = \left\{y \in \mathbb{R}^{n+1} : y_i > 0, \, i = 0, \ldots, n; \, \sum_{i=0}^n y_i = 1\right\} = e_0 \cdots e_n$$

其中 $e_i$ 是标准基向量。$\bar{\Delta}^n$ 表示标准闭 $n$-单纯形。

### 3.8 单纯剖分 (Simplicial Subdivision)

**定义**: 设 $T = x_0 \cdots x_n$ 是 $n$-单纯形。$\bar{T}$ 的**单纯剖分**是满足以下条件的有限单纯形集合 $\{T_i : i \in I\}$：
1. $\bigcup_{i \in I} \bar{T}_i = \bar{T}$
2. 对任意 $i, j \in I$，$\bar{T}_i \cap \bar{T}_j$ 要么为空，要么等于某个公共面的闭包

剖分的**网格** (mesh) 是最大子单纯形的直径。

### 3.10 等边剖分 (Equilateral Subdivision)

对任意正整数 $m$，集合：
$$V = \left\{v \in \mathbb{R}_+^{n+1} : v_j = k_j/m, \, i = 0, \ldots, n; \, \sum_{i=0}^n k_i = m; \, k_i \text{ 整数}\right\}$$
是 $\bar{\Delta}^n$ 的一个单纯剖分的顶点集。该剖分有 $m^n$ 个 $n$-单纯形，直径为 $\sqrt{2}/m$。

### 3.11 重心剖分 (Barycentric Subdivision)

对任意单纯形 $T = x_0 \cdots x_n$，其**重心** $b(T)$ 是：
$$b(T) = \frac{1}{n+1} \sum_{i=0}^n x_i$$

**第一重心剖分**由所有形如 $b(T_0) \cdots b(T_k)$ 的单纯形组成，其中 $T \supset T_0 > T_1 > \cdots > T_k$。

---

## II. Sperner 引理 - Chapter 4

### 4.0 正规标号 (Proper Labeling)

**定义**: 设 $T = \overline{x_0 \cdots x_n}$ 被单纯剖分。设 $V$ 是所有子单纯形顶点的集合。

函数 $\lambda: V \to \{0, \ldots, n\}$ 称为剖分的**正规标号**，如果：
$$\lambda(v) \in \chi(v)$$

其中 $\chi(v)$ 是 $v$ 的载体 (carrier) 的顶点指标集。

如果某子单纯形的顶点集上 $\lambda$ 取遍所有值 $0, \ldots, n$，则称该子单纯形是**完全标号的** (completely labeled)。

### 4.1 Sperner 引理

**定理 (Sperner [1928])**: 设 $T = \overline{x_0 \cdots x_n}$ 被单纯剖分并由函数 $\lambda$ 正规标号。则剖分中**完全标号子单纯形的数目是奇数**。

> 📌 **重要性**: 零不是奇数，所以至少存在一个完全标号的子单纯形！

### 4.2 Sperner 引理的证明 (Kuhn [1968])

**证明** (对 $n$ 归纳):

**基础情形** ($n = 0$): 单纯形只有一个点 $x_0$，它必须标为 0，所以有一个完全标号子单纯形。

**归纳步骤**: 假设对 $n-1$ 成立，证明对 $n$ 成立。

定义：
- $C$ = 所有完全标号 $n$-单纯形的集合
- $A$ = 所有"几乎完全标号" $n$-单纯形的集合（$\lambda$ 的值域恰为 $\{0, \ldots, n-1\}$）
- $B$ = 边界上携带所有标号 $\{0, \ldots, n-1\}$ 的 $(n-1)$-单纯形集合
- $E$ = 所有携带标号 $\{0, \ldots, n-1\}$ 的 $(n-1)$-单纯形集合

**图论论证**:
- 节点集 $D = C \cup A \cup B$
- 边集 $E$
- 定义节点 $d$ 和边 $e$ 关联的规则

**度数计算**:
$$\delta(d) = \begin{cases} 1 & d \in B \cup C \\ 2 & d \in A \end{cases}$$

由于 $\sum_{d \in D} \delta(d) = 2|E|$，得：
$$2|E| = 2|A| + |B| + |C|$$

所以 $|B| + |C|$ 是偶数。由归纳假设 $|B|$ 是奇数，因此 $|C|$ 必须是奇数。∎

---

## III. KKM 引理 - Chapter 5

### 5.1 KKM 定理

**定理 (Knaster-Kuratowski-Mazurkiewicz [1929])**: 设 $\bar{\Delta} = \text{co}\{e_0, \ldots, e_m\} \subset \mathbb{R}^{m+1}$，设 $\{F_0, \ldots, F_m\}$ 是 $\bar{\Delta}$ 的**闭子集**族，满足对任意 $A \subseteq \{0, \ldots, m\}$：
$$\text{co}\{e_i : i \in A\} \subseteq \bigcup_{i \in A} F_i \tag{5.2}$$

则 $\bigcap_{i=0}^m F_i$ **非空且紧**。

### 5.3 KKM 定理的证明

**证明**: 交集显然是紧的（紧集的闭子集）。

设 $\varepsilon > 0$，将 $\bar{\Delta}$ 剖分成直径 $\leq \varepsilon$ 的子单纯形。

对剖分的顶点 $v$ 属于面 $e_{i_0} \cdots e_{i_k}$，由条件 (5.2) 存在某个 $i \in \{i_0, \ldots, i_k\}$ 使得 $v \in F_i$。

如此标号所有顶点，则满足 Sperner 引理的假设，因此存在完全标号子单纯形 $\varepsilon_{p_0} \cdots \varepsilon_{p_m}$，其中 $\varepsilon_{p_i} \in F_i$ 对每个 $i$ 成立。

当 $\varepsilon \to 0$，选取收敛子列 $\varepsilon_{p_i} \to z$。由于 $F_i$ 闭且 $\varepsilon_{p_i} \in F_i$，得 $z \in \bigcap_{i=0}^m F_i$。∎

### 5.4 KKM 引理 (一般形式)

**推论**: 设 $K = \text{co}\{a_0, \ldots, a_m\} \subset \mathbb{R}^k$，设 $\{F_0, \ldots, F_m\}$ 是闭集族满足：
$$\text{co}\{a_i : i \in A\} \subseteq \bigcup_{i \in A} F_i$$

则 $K \cap \bigcap_{i=0}^m F_i$ 非空且紧。

### 5.7 Fan 的推广

**推论 (Fan [1961])**: 设 $X \subset \mathbb{R}^m$，对每个 $x \in X$ 设 $F(x) \subset \mathbb{R}^m$ 闭。假设：
1. 对任意有限子集 $\{x_1, \ldots, x_k\} \subset X$，$\text{co}\{x_1, \ldots, x_k\} \subseteq \bigcup_{i=1}^k F(x_i)$
2. 某个 $x \in X$ 的 $F(x)$ 是紧的

则 $\bigcap_{x \in X} F(x)$ 非空且紧。

---

## IV. Brouwer 不动点定理 - Chapter 6

### 6.1 Brouwer 定理 (标准单纯形版本)

**定理**: 设 $f: \bar{\Delta}^m \to \bar{\Delta}^m$ 连续。则 $f$ 有不动点。

### 6.2 证明

设 $\varepsilon > 0$，将 $\bar{\Delta}$ 单纯剖分成直径 $\leq \varepsilon$ 的子单纯形。

对剖分的顶点 $v \in x_{i_0} \cdots x_{i_k}$，选择：
$$\lambda(v) \in \{i_0, \ldots, i_k\} \cap \{i : f_i(v) \leq v_i\}$$

**该交集非空**：否则对所有 $i \in \{i_0, \ldots, i_k\}$ 有 $f_i(v) > v_i$，则：
$$1 = \sum_{j=0}^m f_j(v) > \sum_{j=0}^k v_{i_j} = \sum_{i=0}^m v_i = 1$$
矛盾。

由 Sperner 引理，存在完全标号子单纯形。即存在单纯形 $\varepsilon_{p_0} \cdots \varepsilon_{p_m}$ 使得 $f_i(\varepsilon_{p_i}) \leq (\varepsilon_{p_i})_i$ 对每个 $i$。

令 $\varepsilon \to 0$，提取收敛子列使 $\varepsilon_{p_i} \to z$。由连续性 $f_i(z) \leq z_i$ 对所有 $i$。

由练习 3.7：若 $x \leq z$ 且 $x, z \in \Delta$，则 $x = z$。故 $f(z) = z$。∎

### 6.6 Brouwer 定理 (紧凸集版本)

**推论**: 设 $K \subset \mathbb{R}^m$ 凸且紧，$f: K \to K$ 连续。则 $f$ 有不动点。

**证明**: 将 $K$ 包含在某个足够大的单纯形 $T$ 中。定义 $h: \bar{T} \to K$ 为将 $x$ 映到 $K$ 中距 $x$ 最近的点（最近点投影）。由 2.7，$h$ 连续且在 $K$ 上为恒等。

则 $f \circ h: \bar{T} \to K \subset \bar{T}$ 有不动点 $z$。这样的不动点不能属于 $\bar{T} \setminus K$（因为 $f \circ h$ 映入 $K$）。故 $z \in K$ 且 $f \circ h(z) = z$，但 $h(z) = z$，所以 $f(z) = z$。∎

### 6.12 等价定理：球面不是球的收缩

**定理**: $\partial B^m$ 不是 $B^m$ 的 r-像（retract）。

**证明**: 假设 $\partial B$ 是 $B$ 的 r-像。则存在连续函数 $g: \partial B \to B$ 和 $h: B \to \partial B$ 使得 $h \circ g$ 是恒等。

定义 $f(x) = g(-h(x))$。则 $f$ 连续且映 $B$ 到自身，由 6.6 有不动点 $z$。即 $z = g(-h(z))$，所以 $h(z) = (h \circ g)(-h(z)) = -h(z)$。

因此 $h(z) = 0 \notin \partial B$，矛盾。∎

---

## V. 等价性 (Some Interconnections) - Chapter 9

### 核心结论

> 📌 **Sperner 引理 ⟺ KKM 引理 ⟺ Brouwer 不动点定理**
>
> 这三个定理在逻辑上是**等价**的！

### 9.1 Brouwer 定理 ⟹ KKM 引理

**证明**: 设 $K = \text{co}\{a_i : i = 0, \ldots, m\}$。假设 $\bigcap_{i=0}^m F_i = \emptyset$。

则 $\{F_i^c\}$ 是 $K$ 的开覆盖，存在从属的单位分解 $f_0, \ldots, f_m$。

定义 $g: K \to K$ 为：
$$g(x) = \sum_{i=0}^m f_i(x) a_i$$

$g$ 连续，由 6.6 有不动点 $z$。

设 $A = \{i : f_i(z) > 0\}$。则 $z \in \text{co}\{a_i : i \in A\}$ 且 $z \notin F_i$ 对所有 $i \in A$。

这与 $\text{co}\{a_i : i \in A\} \subseteq \bigcup_{i \in A} F_i$ 矛盾。∎

### 9.3 KKM 引理 ⟹ Brouwer 定理

**证明 (KKM [1929])**: 设 $f: \bar{\Delta}^m \to \bar{\Delta}^m$ 连续。

定义 $F_i = \{z \in \bar{\Delta} : f_i(z) \leq z_i\}$。

**验证 KKM 条件**: 设 $z \in e^{i_0} \cdots e^{i_k}$。则：
$$\sum_{j=0}^m f_j(z) = \sum_{j=0}^k z_{i_j}$$
故至少存在某个 $f_{i_j}(z) \leq z_{i_j}$，即 $z \in F_{i_j}$。

因此 $\text{co}\{e^i : i \in A\} \subseteq \bigcup_{i \in A} F_i$。

每个 $F_i$ 闭（$f$ 连续）。由 KKM 引理：
$$\bigcap_{i=0}^m F_i \neq \emptyset$$

但 $\bigcap_{i=0}^m F_i = \{x \in \bar{\Delta} : f(x) \leq x\}$，即不动点集。∎

### 9.4 KKM 引理 ⟹ 均衡定理 (8.3)

**证明 (Gale [1955])**: 定义 $F_i = \{p \in \bar{\Delta} : f_i(p) \leq 0\}$。

若 $p \in \text{co}\{e^{i_0}, \ldots, e^{i_k}\}$，则不能对所有 $j$ 有 $f_{i_j}(p) > 0$，否则：
$$p \cdot f(p) = \sum_{j=0}^k p_{i_j} f_{i_j}(p) > 0$$
与 Walras 定律矛盾。

故 KKM 条件满足，$\{p : f(p) \leq 0\} = \bigcap_{i=0}^m F_i$ 非空且紧。∎

### 9.5 均衡定理 ⟹ Brouwer 定理

**证明 (Uzawa [1962])**: 设 $f: \bar{\Delta}^m \to \bar{\Delta}^m$ 连续。定义：
$$g(x) = f(x) - \frac{x \cdot f(x)}{x \cdot x} x$$

则 $g$ 连续且 $x \cdot g(x) = 0$ 对所有 $x$（$g$ 将 $f(x)$ 投影到过原点且与 $x$ 正交的超平面）。

由 8.3，存在 $\bar{p} \in \bar{\Delta}$ 使 $g(\bar{p}) \leq 0$。

若 $\bar{p}_i = 0$，则 $f_i(\bar{p}) \leq 0$，但 $f_i(\bar{p}) \geq 0$（因 $f(\bar{p}) \in \bar{\Delta}$），故 $f_i(\bar{p}) = 0$。

若 $\bar{p}_i > 0$，则由 $\bar{p} \cdot g(\bar{p}) = 0$ 和 $g(\bar{p}) \leq 0$ 得 $g_i(\bar{p}) = 0$。

综上，$g(\bar{p}) = 0$ 蕴含 $\bar{p} = f(\bar{p})$。∎

### 9.8 Fan 引理 ⟹ Brouwer 定理

**证明 (Fan [1969])**: 设 $f: K \to K$ 连续。对每个 $x$ 定义：
$$U(x) = \{y : |y - f(x)| < |x - f(x)|\}$$

则 $U(x)$ 凸，$x \notin U(x)$，且 $U$ 有开图。

若 $x$ 是 $U$-极大的，则对所有 $y \in K$：$|x - f(x)| \leq |y - f(x)|$。

取 $y = f(x)$ 得 $|x - f(x)| = 0$，即 $f(x) = x$。

反之，若 $x$ 是不动点，则 $U(x) = \{y : |y - f(x)| < 0\} = \emptyset$。

由 7.5，不动点集非空且紧。∎

### 9.16 Brouwer 定理 ⟹ Sperner 引理（弱形式）

**练习**: 证明存在至少一个完全标号子单纯形。

**提示**: 对剖分的顶点首先定义映射 $f$。若顶点标号为 $i$，则 $f$ 应将其移离 $x_i$。然后在每个子单纯形上线性延拓 $f$。

若子单纯形完全标号，则所有点都向重心移动，重心保持不动。若不完全标号，则所有点都被移动。

因此唯一的不动点是完全标号子单纯形的重心，由 Brouwer 定理至少存在一个。∎

---

## VI. 等价性关系图

```
                    Sperner's Lemma
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
       KKM Lemma ◄─────────────► Brouwer FPT
            │                           │
            │         ┌─────────────────┤
            ▼         ▼                 ▼
      Fan's Lemma  Equilibrium     Retraction
         (7.5)     Theorem (8.3)   Theorem (6.12)
```

### 具体蕴含关系

| 出发定理 | 推出定理 | 参考节 |
|---------|---------|-------|
| **Brouwer** | KKM | 9.1, 9.2 |
| **KKM** | Brouwer | 9.3 |
| **KKM** | 均衡定理 | 9.4 |
| **均衡定理** | Brouwer | 9.5 |
| **Fan 引理** | Brouwer | 9.8 |
| **Fan 引理** | 均衡定理 | 9.7 |
| **Brouwer** | Fan 引理 | 9.12 |
| **Brouwer** | Sperner (弱) | 9.16 |

---

## VII. 应用提要

### 10.2 完全标号子单纯形与 KKM 引理

**定理**: 设 $\{F_0, \ldots, F_m\}$ 满足 KKM 引理假设，$F = \bigcap_{i=0}^m F_i$。

对任意 $\varepsilon > 0$ 存在 $\delta > 0$，使得若剖分网格 $< \delta$，则每个完全标号子单纯形位于 $N_\varepsilon(F)$ 中。

### 10.5 逼近不动点

**定理**: 设 $f: \bar{\Delta} \to \bar{\Delta}$ 连续，$F = \{z : f(z) = z\}$。

对任意 $\varepsilon > 0$ 存在 $\delta > 0$，使得若剖分网格 $< \delta$，则每个完全标号子单纯形位于 $N_\varepsilon(F)$ 中。

### 10.7 近似不动点

**定理 (Green [1981])**: 设 $f$ 满足 Brouwer 定理假设，$F$ 为不动点集。

对任意 $\varepsilon > 0$ 存在 $\delta > 0$，使得 $|f(z) - z| < \delta$ 蕴含 $z \in N_\varepsilon(F)$。

---

## VIII. 总结

### 三大定理的本质

| 定理 | 本质内容 |
|------|---------|
| **Sperner** | 组合/离散：正规标号剖分必有完全标号子单纯形 |
| **KKM** | 拓扑/覆盖：满足覆盖条件的闭集族有非空交 |
| **Brouwer** | 分析/不动点：紧凸集到自身的连续映射有不动点 |

### 等价性的意义

1. **统一性**: 看似不同领域的三个定理实际上**逻辑等价**
2. **灵活性**: 可以选择最适合当前问题的形式
3. **计算性**: Sperner 引理提供了**算法基础**（路径跟踪法）
4. **应用性**: 经济学中的均衡存在性、博弈论中的 Nash 均衡等都可从这些定理推出

---

## IX. 参考文献

- Sperner, E. (1928). "Neuer Beweis für die Invarianz der Dimensionszahl und des Gebietes". *Abh. Math. Sem. Univ. Hamburg*, 6:265-272.
- Knaster, B., Kuratowski, C., Mazurkiewicz, S. (1929). "Ein Beweis des Fixpunktsatzes für n-dimensionale Simplexe". *Fund. Math.*, 14:132-137.
- Brouwer, L.E.J. (1912). "Über Abbildung von Mannigfaltigkeiten". *Math. Ann.*, 71:97-115.
- Fan, K. (1961). "A Generalization of Tychonoff's Fixed Point Theorem". *Math. Ann.*, 142:305-310.
- Gale, D. (1955). "The Law of Supply and Demand". *Math. Scand.*, 3:155-169.
- Uzawa, H. (1962). "Walras' Existence Theorem and Brouwer's Fixed Point Theorem". *Econ. Studies Quarterly*, 13:59-62.
